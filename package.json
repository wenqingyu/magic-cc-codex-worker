{
  "name": "magic-cc-codex-worker",
  "version": "0.2.5",
  "description": "Parallel Codex workers inside Claude Code — multi-agent orchestration.",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "plugin/dist/index.js",
  "bin": {
    "codex-team-mcp": "plugin/dist/index.js"
  },
  "scripts": {
    "build": "tsc && rm -rf plugin/dist/roles/defaults && mkdir -p plugin/dist/roles && cp -r src/roles/defaults plugin/dist/roles/defaults",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf plugin/dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "execa": "^9.5.1",
    "nanoid": "^5.0.9",
    "smol-toml": "^1.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
