node_modules/
# plugin/dist/ is committed so /plugin install works without a build step.
# CI verifies plugin/dist/ stays in sync with src/ via `npm run build`.
.codex-team/
*.log
.DS_Store
.env
.env.local
.magic-codex/
